"use client";
import { useEffect,useState } from "react";

type Pet={name:string;species:"dog"|"cat";breed:string;age:string;weightKg:string;color:string;notes:string};
const emptyPet=():Pet=>({name:"",species:"dog",breed:"",age:"",weightKg:"",color:"",notes:""});
const field="h-11 w-full rounded-xl border border-slate-200 bg-white px-3 text-sm outline-none focus:border-emerald-500 focus:ring-4 focus:ring-emerald-500/10";
export function PublicOnboardingForm({token}:{token:string}){const [link,setLink]=useState<{valid:boolean;status:string;organization_name?:string}|null>(null);const [pets,setPets]=useState<Pet[]>([emptyPet()]);const [form,setForm]=useState({customerName:"",phone:"",addressLine:"",kabupatenKota:"",kecamatan:"",customerNotes:"",latitude:"",longitude:""});const [state,setState]=useState<"idle"|"sending"|"done"|"error">("idle");
useEffect(()=>{fetch(`/api/onboarding/${encodeURIComponent(token)}`).then(r=>r.json()).then(setLink).catch(()=>setLink({valid:false,status:"invalid"}))},[token]);
if(!link)return <PublicScreen icon="…" title="Memuat pendaftaran" text="Mohon tunggu sebentar."/>;if(!link.valid)return <PublicScreen icon={link.status==="expired"?"⏰":"🔗"} title={link.status==="expired"?"Link kedaluwarsa":link.status==="submitted"?"Link sudah digunakan":"Link tidak valid"} text="Hubungi bisnis untuk mendapatkan link pendaftaran baru."/>;if(state==="done")return <PublicScreen icon="🎉" title="Terima kasih!" text="Data Anda dan anabul sudah diterima dan akan diperiksa sebelum masuk ke CRM."/>;
const patch=(key:string,value:string)=>setForm(c=>({...c,[key]:value}));const patchPet=(i:number,key:keyof Pet,value:string)=>setPets(c=>c.map((p,n)=>n===i?{...p,[key]:value}:p));async function submit(){if(form.customerName.trim().length<2||form.phone.replace(/\D/g,"").length<7||!form.addressLine.trim()||pets.some(p=>!p.name.trim())){setState("error");return}setState("sending");const response=await fetch(`/api/onboarding/${encodeURIComponent(token)}`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({...form,latitude:form.latitude?Number(form.latitude):null,longitude:form.longitude?Number(form.longitude):null,pets:pets.map(p=>({...p,weightKg:p.weightKg||null}))})});setState(response.ok?"done":"error")}
return <main className="min-h-screen bg-[#f4f7f6] px-4 py-8"><div className="mx-auto max-w-xl"><div className="rounded-3xl bg-[#0f1b2d] p-6 text-white"><p className="text-xs font-bold uppercase tracking-[.16em] text-[#57c9ad]">{link.organization_name??"Operro"}</p><h1 className="mt-2 text-2xl font-bold">Pendaftaran pelanggan baru</h1><p className="mt-2 text-sm text-slate-300">Isi sekali dari ponsel. Admin akan memeriksa data sebelum menyimpannya.</p></div><div className="mt-5 space-y-5 rounded-3xl border bg-white p-5 shadow-sm sm:p-7"><h2 className="font-bold">Data pemilik</h2>{([['customerName','Nama lengkap'],['phone','Nomor WhatsApp'],['addressLine','Alamat lengkap'],['kabupatenKota','Kabupaten/Kota'],['kecamatan','Kecamatan']] as const).map(([key,label])=><label key={key} className="block text-xs font-semibold text-slate-600">{label}<input value={form[key]} onChange={e=>patch(key,e.target.value)} className={`${field} mt-1`} /></label>)}<div className="grid gap-3 sm:grid-cols-2"><label className="text-xs font-semibold text-slate-600">Latitude (opsional)<input value={form.latitude} onChange={e=>patch('latitude',e.target.value)} className={`${field} mt-1`} inputMode="decimal"/></label><label className="text-xs font-semibold text-slate-600">Longitude (opsional)<input value={form.longitude} onChange={e=>patch('longitude',e.target.value)} className={`${field} mt-1`} inputMode="decimal"/></label></div><label className="block text-xs font-semibold text-slate-600">Catatan lokasi atau preferensi<textarea value={form.customerNotes} onChange={e=>patch('customerNotes',e.target.value)} className="mt-1 min-h-20 w-full rounded-xl border p-3 text-sm"/></label><h2 className="pt-3 font-bold">Anabul</h2>{pets.map((pet,i)=><section key={i} className="space-y-3 rounded-2xl bg-slate-50 p-4"><div className="flex justify-between"><strong className="text-sm">Anabul {i+1}</strong>{pets.length>1?<button onClick={()=>setPets(c=>c.filter((_,n)=>n!==i))} className="text-xs font-bold text-rose-600">Hapus</button>:null}</div><div className="grid gap-3 sm:grid-cols-2"><input className={field} value={pet.name} onChange={e=>patchPet(i,'name',e.target.value)} placeholder="Nama"/><select className={field} value={pet.species} onChange={e=>patchPet(i,'species',e.target.value)}><option value="dog">Anjing</option><option value="cat">Kucing</option></select><input className={field} value={pet.breed} onChange={e=>patchPet(i,'breed',e.target.value)} placeholder="Ras"/><input className={field} value={pet.weightKg} onChange={e=>patchPet(i,'weightKg',e.target.value)} placeholder="Berat kg" inputMode="decimal"/><input className={field} value={pet.color} onChange={e=>patchPet(i,'color',e.target.value)} placeholder="Warna bulu"/><input className={field} value={pet.age} onChange={e=>patchPet(i,'age',e.target.value)} placeholder="Umur"/></div><textarea className="min-h-20 w-full rounded-xl border p-3 text-sm" value={pet.notes} onChange={e=>patchPet(i,'notes',e.target.value)} placeholder="Catatan khusus untuk groomer"/></section>)}{pets.length<5?<button type="button" onClick={()=>setPets(c=>[...c,emptyPet()])} className="w-full rounded-xl border-2 border-dashed border-emerald-300 py-3 text-sm font-bold text-emerald-700">+ Tambah anabul</button>:null}{state==="error"?<p className="rounded-xl bg-rose-50 p-3 text-xs font-semibold text-rose-700">Periksa nama, WhatsApp, alamat, dan nama setiap anabul.</p>:null}<button type="button" onClick={submit} disabled={state==="sending"} className="h-12 w-full rounded-xl bg-emerald-700 text-sm font-bold text-white disabled:opacity-50">{state==="sending"?"Mengirim…":"Kirim ke admin"}</button><p className="text-center text-[11px] text-slate-400">Data baru masuk ke CRM setelah disetujui admin.</p></div></div></main>}
function PublicScreen({icon,title,text}:{icon:string;title:string;text:string}){return <main className="grid min-h-screen place-items-center bg-[#f4f7f6] p-6"><div className="max-w-sm text-center"><div className="text-5xl">{icon}</div><h1 className="mt-4 text-xl font-bold">{title}</h1><p className="mt-2 text-sm text-slate-500">{text}</p></div></main>}
