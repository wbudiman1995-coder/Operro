# Codex review of 86ad714 — not approved for integration

The committed gate 7/8 logs contain the expected successful integration and concurrency results. This review did not rerun those commands. The following code findings remain despite those passing scenarios. Make focused corrections on this branch; do not rebuild sections 23–26.

1. **Upgrade migration fails when purchase ledger rows already exist.** `20260926090000_membership_renewal_billing_and_reconciliation.sql:55` updates `customer_package_ledger`, but `20260721000700_customer_programs.sql:190` installs `trg_cpl_block_update`, whose function unconditionally raises `restrict_violation`. Fresh-schema replay matches zero rows and misses the failure. Add an upgrade test: apply through the prior migration, create a real package invoice/purchase, then apply this migration. Preserve ledger immutability for application callers; use a narrowly scoped migration strategy or derive historical provenance without updating immutable rows.

2. **Rollover configuration is ignored.** `package-manager.tsx:52` offers `none` and `rollover`, but renewal always adds `pkg.total_sessions` to the existing balance. Either implement both policies safely (with immutable movements and protection for held sessions), or remove the unsupported choice and constrain/document the one supported policy. Test positive remaining balance, active holds, expired membership, and both catalog settings. Do not silently discard reserved or paid-for sessions.

3. **Renewal retries depend on mutable catalog state instead of the original request.** At migration line 188, a legitimate retry fails if the catalog price changed after the original success. Conversely, changed invoice dates/due dates/notes are accepted with the same key. Persist and compare normalized request inputs, return the original result for an identical retry after catalog changes, and reject changed material inputs. Show a renewal preview (price, sessions, currency, pet/service scope, expiry) and validate the accepted terms before issuing; the current form only asks for branch/dates. Catalog changes to service/per-pet terms must not charge for one product while topping up an older incompatible entitlement. Add behavior tests, not just source regexes.

4. **Invoice reconciliation only checks for NULL linkage.** At migration lines 325–329, any non-null `invoice_id` passes; the FK guarantees only organization/existence. Verify the linked invoice belongs to the correct customer and purchase/renewal, has matching package lines and session snapshots, and has a relevant status. Add a same-organization, wrong-customer/wrong-package invoice fixture: it must be flagged. Also test missing reversal links after a consumed reservation is reversed and duplicate consumption links, rather than relying on equal counts or merely non-null IDs.

The original requested guarded purchased-term editing remains intentionally omitted in the handoff, so do not claim every requested item is complete. Either implement a safe audited path with explicit allowed fields/revision checks, or clearly retain that item as incomplete. Do not disguise missing functionality as a minor risk.

Update the existing single handoff with exact results and remaining gaps. Keep the branch isolated; no merge/deploy yet. Add permanent regression coverage for the populated upgrade and retry-after-catalog-change cases. Run focused checks, then the final app checks, both SQL smoke tests, and affected PostgreSQL 16 integration/concurrency scenarios. Record browser verification separately from compilation/SQL evidence.
